module operators
{
    chare add_vector {
        entry add_vector(ck::future<std::vector<double>>, ck::future<std::vector<double>>, ck::future<std::vector<double>>);
    };

    chare loader {
        entry loader(ck::future<std::vector<double> >, ck::future<std::vector<double> >);
    }
}